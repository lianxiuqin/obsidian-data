# 架构愿景

状态：🟡 初步设计（愿景与分层；其中「前端加载」「热重载」未实现）

cordis-tavern 项目的参考架构建立在两个核心技术之上：**SillyTavern** 提供了成熟的 LLM 前端交互模型和丰富的功能体系，**Cordis** 提供了可靠的插件化内核和动态组合能力。两者的结合旨在构建一个既能复用 SillyTavern 已验证的交互模式，又具备现代化插件架构的通用 AI 聊天平台。

---

## 上游一：SillyTavern

SillyTavern 的交互模型与功能体系（模型兼容层、角色卡、世界书、提示词工程、沉浸式体验、扩展生态）**不在这里复述** —— 原始资料见 [[项目文档/项目设计文档/来源参考/sillytavern|来源参考/sillytavern]]。

本项目要复用的是它已验证的功能体系与交互模式；要替换的是它那套没有形式化插件契约、没有依赖管理的 `extensions/` 扩展系统。

---

## 上游二：Cordis

Cordis 的核心概念（Plugin / Context / inject / Events / Effect）、Fiber 状态机与技术特性**不在这里复述** —— 原始资料见 [[项目文档/项目设计文档/来源参考/cordis|来源参考/cordis]]。

本项目只依赖它的三件事：生命周期管理、插件解析（拓扑排序）、上下文传播；其余全在插件层。

---

## 两者结合：cordis-tavern 的架构愿景

### 重构动机

SillyTavern 的扩展系统虽然灵活，但缺乏形式化的插件契约和依赖管理。插件之间没有声明式的依赖关系，生命周期控制依赖手动管理，扩展的发现和加载缺乏统一机制。

**cordis-tavern 的核心目标**：保留 SillyTavern 经过验证的功能体系和交互模式，用 Cordis 的插件架构替换其内部的扩展系统，从而获得：

- **声明式依赖管理**：插件通过 `inject` 声明依赖，Cordis 自动处理加载顺序
- **可靠的生命周期**：插件的注册即副作用，卸载即回滚，无需手动清理
- **动态组合能力**：插件可安全地在运行时加载和卸载（热重载**尚未实现**，见 [[项目文档/开发文档/待办|待办]]）
- **类型安全的服务访问**：通过 TypeScript 声明合并，服务访问在编译时即可验证

### 架构分层

```
┌─────────────────────────────────────────────────┐
│                  前端 UI 层                       │
│         (角色卡、聊天、设置等交互界面)              │
│                   ※ 后续阶段                      │
├─────────────────────────────────────────────────┤
│              插件功能层                            │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐  │
│  │ 角色卡 │ │ 世界书 │ │ 提示词 │ │ TTS  │ │ RAG  │  │
│  │ 管理  │ │ 管理  │ │ 工程  │ │ 集成 │ │ 数据银行│ │
│  └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘  │
│         ※ 以独立 npm 包形式提供，不在仓库内        │
├─────┼────────┼────────┼────────┼────────┼───────┤
│              插件契约层 (ct 声明)                  │
│  package.json → exports["."].source + ct.*        │
├─────────────────────────────────────────────────┤
│              启动与构建                            │
│  ┌──────────┐ ┌──────────┐                       │
│  │ 启动层    │ │ 构建工具  │                       │
│  │(bootstrap)│ │(esbuild) │                       │
│  └────┬─────┘ └──────────┘                       │
├───────┼─────────────────────────────────────────┤
│              Cordis 内核                             │
│  Context · Fiber · Service · Events · Effect        │
│  (生命周期管理 · 插件解析 · 上下文传播 · 拓扑排序)   │
├─────────────────────────────────────────────────┤
│              存储层 · CLI 层                       │
│     环境变量 (.env) · 命令行工具                    │
└─────────────────────────────────────────────────┘
```

> **说明**：
> - 原图中的「插件注册表/环境表」已移除 —— 拓扑排序与加载由 `@cordis-tavern/plugin-loader` 负责，不另建注册表。
> - 「插件功能层」的插件以独立 npm 包形式提供，由 pnpm 安装到 `$CT_HOME`（全局根，或 `profile/<env>/` 环境私有根），**不在仓库内**。
> - 「前端 UI 层」与上列业务插件属后续阶段，**当前均未交付**（详见 [[项目文档/开发文档/现状|现状]]）。

各层与代码落点：

| 层 | 代码落点 | 设计文档 |
| --- | --- | --- |
| 启动层 | `bootstrap/index.ts`（`start`）、`bootstrap/actions/assemble.ts` | [[项目文档/项目设计文档/内核设计/启动层\|启动层]] |
| CLI 层 | `bootstrap/cli/`（`ct` 命令、命令注册表） | [[项目文档/项目设计文档/内核设计/CLI层\|CLI层]] |
| 插件契约 | `bootstrap/cli/actions/contract.ts`、`scripts/build-lib.mjs` | [[项目文档/项目设计文档/插件设计/插件契约层\|插件契约层]] |
| 构建工具 | `scripts/`（`build.mjs` / `build-dist.mjs` / `dev.mjs` / `vendor*.mjs`） | [[项目文档/项目设计文档/内核设计/自定义构建工具\|自定义构建工具]] |
| 存储层 | `$CT_HOME` 的数据文件夹布局 | [[项目文档/项目设计文档/内核设计/存储层\|存储层]] |
| 插件功能层 | **不在仓库内**：独立 npm 包，装在 `$CT_HOME` 的 node_modules | [[项目文档/项目设计文档/插件设计/插件概念总览\|插件概念总览]] |

### 关键设计决策

| 维度 | SillyTavern 原有 | cordis-tavern 方案 |
|---|---|---|
| 插件发现 | 扫描 extensions/ 目录 | `package.json` 的 `ct` 字段声明 |
| 依赖管理 | 手动 / 无 | Cordis `inject` 声明 + 自动拓扑 |
| 生命周期 | 手动 `onLoad` / `onUnload` | Cordis Fiber 状态机自动管理 |
| 服务注册 | 全局对象挂载 | `ctx.provide()` + Context 作用域 |
| 事件通信 | 自定义 EventEmitter | Cordis 类型化事件（5 种模式） |
| 前端加载 | `<script>` 标签顺序 | `ct.client` 声明 + `window.boot` 启动图（由 host 注入静态页面）——**规划中，当前未实现** |
| 热重载 | 不支持 | Cordis Effect 回滚 + 重载（**规划中，当前未实现**） |

### 插件契约

插件是**模块化的 npm 包**：身份与入口用 npm 标准字段，npm 表达不了的语义用 `ct` 扩展字段，二者并存。
字段定义、校验规则、包命名正典**只写在** [[项目文档/项目设计文档/插件设计/插件契约层|插件契约层]]。

---

## 开发阶段

三个阶段（总体 / 中层 / 拓展）见 → [[项目文档/项目总览|项目总览]]。
