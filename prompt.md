1. 修正原文中的错别字、语病，并重新理顺分段，使阅读逻辑通畅。
    
2. 使用 Markdown 标题层级（`#`、`##`）组织内容结构，将核心概念用**加粗**标记。
    
3. 提取 1-3 个核心标签，写入 YAML Frontmatter 的 `tags` 字段。
    
4. 自动识别文中提到的专业概念，使用 Obsidian 的双链语法 `[[概念]]` 进行标记。
    
5. 在文末生成一个总结模块，使用2-3句话总结文档的大致内容。